# Normalize text files and keep binary assets untouched.
* text=auto
*.java text
*.md text
*.xml text
*.gitignore text
*.gitattributes text

*.db binary
*.jar binary
*.class binary

# Keep the local database file on merges if it changes.
src/main/resources/rpg.db merge=ours
